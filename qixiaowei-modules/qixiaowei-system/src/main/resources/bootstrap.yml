# Tomcat
server:
  port: 19201

# Spring
spring:
  application:
    # 应用名称
    name: qixiaowei-system
  profiles:
    # 环境配置(local本地-用于本地启动，dev开发，test测试, prod生产)
    active: @profiles.active@

logging:
  level:
    net.qixiaowei.system.mapper: debug
