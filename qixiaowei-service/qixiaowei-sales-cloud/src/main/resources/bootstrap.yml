# Tomcat
server:
  port: 19301

# Spring
spring:
  application:
    # 应用名称
    name: qixiaowei-sales-cloud
  profiles:
    # 环境配置(local本地-用于本地启动，dev开发，test测试, prod生产)
    active: @profiles.active@

logging:
  level:
    net.qixiaowei.sales.cloud.mapper: debug
