# Tomcat
server:
  port: 19304

# Spring
spring:
  application:
    # 应用名称
    name: qixiaowei-wisdom-cloud
  profiles:
    # 环境配置(local本地-用于本地启动，dev开发，test测试, prod生产)
    active: @profiles.active@

logging:
  level:
    net.qixiaowei.system.mapper: debug
